import 'package:flutter/material.dart';
import 'junction/junction_screen.dart' as new_junction;

class JunctionScreen extends StatelessWidget {
  const JunctionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const new_junction.JunctionScreen();
  }
}
